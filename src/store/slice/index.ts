import { createSlice } from "@reduxjs/toolkit";

const initialState: {
  /* ТИПЫ СТЭЙТА*/
} = {
  /* СТЭЙТ */
};

const /* ИМЯ ФУНКЦИИ СЛАЙСА */ = createSlice({
  name: "/* ИМЯ СЛАЙСА */",
  initialState,
  reducers: {
    /* ИМЯ РЕДЮСЕРА */(state, action: { payload: string; type: string }) {
    },
  },
});

export const /* ИМЯ СЛАЙСА */Reducer = /* ИМЯ ФУНКЦИИ СЛАЙСА */.reducer;
export const {
  /* ИМЯ РЕДЮСЕРА */,
  /* ИМЯ РЕДЮСЕРА */,
} = /* ИМЯ ФУНКЦИИ СЛАЙСА */.actions;
